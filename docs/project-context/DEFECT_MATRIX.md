# Corrective defect matrix — 2026-09-12

Status is evidence-based. Passing prior tests did not close these defects.

| Requirement | Previous failure | Root cause | Files changed | Regression | Final result |
|---|---|---|---|---|---|
| Successful analytical request must persist identifiers | Dashboard returned a run UUID absent in a new session | `get_db` closed without commit; tests hid this with a shared session | `app/db/session.py`, `app/api/deps.py`, `app/services/analysis.py`, `app/services/dashboard.py`, `app/api/routes/dashboard.py` | `test_dashboard_snapshot_exists_in_a_new_session` uses real `get_db` + a second session | Snapshot and values exist after commit |
| Failed generation must not leave a completed snapshot | Partial rows could flush then roll back inconsistently | No explicit write unit of work | Same + `test_failed_dashboard_leaves_no_completed_snapshot` | Forced persist failure | No completed snapshot row |
| Exports/AI must use the displayed snapshot | Endpoints silently recalculated | `create_export`/`run_ai_task` called `build_dashboard` | `publishing.py`, `ai_gateway.py`, export/AI routes, frontend API | `test_export_and_ai_require_matching_snapshot`, `test_historical_export_does_not_follow_later_raw_changes` | 422/409 on mismatch; historical export unchanged |
| Formula version for the requested period | Only `is_current` versions were used | `run_calculation` ignored validity dates | `calculation.py` | `test_historical_formula_version_is_selected` | Period-valid version selected |
| Safe hierarchy prefix queries | `path.startswith` became SQL `LIKE` wildcards | `_` and `%` in codes | `geography.py` | `test_like_wildcard_codes_do_not_expand_subtrees` | Underscore/percent codes isolated |
| Effective-date org-unit mappings | Latest/any mapping won | Date ignored; overlaps allowed | `geography.py`, `sync.py` | date-aware + overlap tests | Historical remap preserved |
| MPDSR event scope | Any tracker event with the same `event_type` counted | No programme/program UID filter | `mpdsr_events.py`, `calculation.py`, `modules.py` | `test_mpdsr_ignores_foreign_program_events` | Foreign program events excluded |
| Quality lifecycle | Flags were list/get only | No acknowledge/resolve/reopen/suppress | `quality.py` | Route contract added | Admin-permissioned mutations |
| Production CORS | Localhost always allowed | Hard-coded origins | `config.py`, `main.py`, `security.py` | `test_production_cors_does_not_auto_permit_localhost` | Localhost stripped outside dev/test |
| Gold-standard tests | Numerators derived from expected percentages | Tautological fixture loader | `acholi_gold_standard.json`, phase 3/7 tests | Independent `VERIFIED FIXTURE` sources | Same expected outputs from explicit counts |
| Alembic head | Schema corrections would have rewritten 0001–0005 | Missing forward revision | `0006_corrective_snapshots.py` | head + 0005→0006 tests | Forward-only 0006 |
| Facility population race | Two connections could both mark current | Prior row lock only when a current row existed | `population.py`, `0006` login index, concurrency test | `test_postgres_two_connection_facility_population_approval` | Exactly one current approved row |
| Exports generated in the API request | No queued/worker boundary | `create_export` wrote files immediately | `publishing.py`, workers, frontend poll | `test_export_stays_queued_until_worker_runs` | Queued until worker; eager only when configured |
| Maps used first object value / SVG-only | `Object.values(...)[0]` and no production map library | Generic dashboard map | `AdaptiveMap.tsx`, `MapLibreCanvas.tsx` | Playwright selector/snapshot test plus typecheck/build | Selected indicator colours MapLibre features |

## Material amendment — 2026-09-13

Results are from tests executed on 2026-09-13 (SQLite plus the disposable PostgreSQL 18 cluster on port 55432). Passing tests do not constitute owner acceptance.

| § | Requirement | Previous failure | Root cause | Main files changed | Regression tests | Result |
|---|---|---|---|---|---|---|
| 1 | Analytical execution must not happen on GET | `GET /analytics/dashboard` and `GET /analytics/modules/*` created runs and snapshots; `GET /exports/jobs/{id}/file` wrote audit rows | Mutations behind safe methods (CSRF-exempt) | `api/routes/dashboard.py`, `modules.py`, `analysis_snapshots.py`, `exports.py`, `services/analysis.py`, migration 0007, frontend `api.ts`, `DashboardView.tsx`, pages | `test_amendment_execution_contract.py` (every GET route leaves the DB byte-identical; CSRF 403; request-key reuse adds no runs; 409 conflict; per-user keys; owner-only snapshot; permissions re-checked; AI omitted-module bypass closed; IDs in a new session; failed execution leaves no runs/values/flags/snapshot), Vitest `execution.test.tsx`, Playwright "POST once, refresh re-opens snapshot" | Passed |
| 2 | Workbook identity and N.5 regressions | Acholi gold fixture used 1,000,000 population and back-calculated counts; §14.1 PMR/MMR untested | No verified population source | `services/population_workbook.py`, `scripts/import_population_workbook.py` | `test_population_workbook.py` (checksum, 146/135/11, 1,022 cells, totals, N.5 values, file unchanged), `test_amendment_acholi_n5_gold.py` (ANC1 97.4/95.4, institutional delivery 71.4/67.6, PMR 20.4, MMR 80.1 from district/city populations) | Passed |
| 3 | Reviewed crosswalk, no fuzzy matching | No alias mechanism | — | `population_workbook.py`, `PopulationSourceAlias` | `test_population_workbook.py` (exact/unmatched/type mismatch/ambiguous/duplicate target/pending/rejected/approved; second reviewer; audit; drafts only; national total opt-in; district+city children sum) | Passed |
| 4 | No invented sub-annual population year | FY rules silently applied to quarters and months | Rule lookup by parent FY | `services/population.py`, `models`, migration 0007, `seed.py` | `test_amendment_population.py`, updated quarter tests | Passed; rule recorded as open |
| 5 | Facility catchment is facility-only | Child sums could include facility entries | Child-sum helper mixed sources | `services/population.py` | `test_amendment_population.py` | Passed |
| 6 | Verified zero vs unknown MPDSR | Missing events counted as zero | No coverage evidence | `services/event_coverage.py`, `calculation.py`, `sync.py` | `test_amendment_mpdsr_coverage.py`, `test_amendment_sync_freshness.py` (window through extraction verifies; period-only window does not) | Passed |
| 7 | One common numerator/denominator scope | Parent numerator combined with child denominator | Components resolved independently | `calculation.py`, `quality.py` | `test_amendment_aggregation_scope.py` | Passed |
| 8 | District/city peers; no district+facility mix | Cities treated as a different level; nested double counting possible | Level rank comparison | `domain/enums.py`, `services/geography.py`, `calculation.py`, `quality.py` | `test_amendment_aggregation_scope.py` | Passed |
| 9 | Direction-aware findings and rankings | Rankings sorted raw values; AI called any increase an improvement | No interpretation layer | `domain/interpretation.py`, `modules.py`, `dashboard.py`, `ai_gateway.py`, `KpiCard.tsx` | `test_amendment_interpretation.py` (every mode, BLUE/missing, precision, ranking exclusions, unsafe indicators, API ranking) | Passed |
| 10 | Governed MPDSR causes | Cause counts exposed below region and without suppression | No disclosure policy | `modules.py`, `config.py` | `test_amendment_mpdsr_causes.py` | Passed |
| 11 | Blame/causation protection | Narrow keyword list; free-text provider output | — | `evidence.py`, `ai_gateway.py`, `providers.py` | `test_amendment_ai_safety.py` (all listed phrases via API, unsafe/invalid provider output falls back, valid statements accepted) | Passed |
| 12 | Freshness ordering | Freshness written before `finished_at`; first observation used | Call order in sync runners | `services/sync.py`, `routes/sync.py`, `quality.py` | `test_amendment_sync_freshness.py` | Passed |
| 13 | Map cohort | Client joined a separately fetched layer at a client-computed `as_of`; district shapes could stand in for regions | Map not tied to the snapshot | `services/geometry.py`, `dashboard.py`, `analysis_snapshots.py`, `AdaptiveMap.tsx`, `MapLibreCanvas.tsx` | `test_amendment_map_cohort.py` | Passed |
| 14 | Prototype blacklist | — | — | `UI_VISUAL_CONTRACT.md`; client-side value sorting removed | Review; no Google Fonts in `src` | Documented |
| 15 | Export completeness and labels | Blank governance columns; PPTX truncated to the first rows; mismatched labels | Writers read sparse evidence | `domain/exports.py`, `publishing.py`, `routes/exports.py`, `dashboard.py` | `test_amendment_exports.py`, `test_phase6_publishing.py` | Passed |
| — | Formula coverage matrix | EPI indicators reported as `approved` classification | Non-empty `{"mode": "unclassified"}` treated as approval | `test_formula_coverage.py`, `FORMULA_COVERAGE_MATRIX.json` | Full-matrix equality plus TBD assertion | Passed (24 approved, 30 TBD, 6 counts) |
| — | ESLint gate | 420 errors from generated `.next-gate` | Output directory not ignored | `eslint.config.mjs` | `npx eslint .` | 0 errors |

Remaining unverified or blocked items are listed in `OPEN_ITEMS.md`.

## Pre-DHIS2 productionisation — 2026-09-14

Results are from tests executed on 2026-09-14. SQLite unless marked PostgreSQL (disposable 18.1 cluster, port 55432). Passing tests are not owner acceptance.

| WP | Requirement | Defect or gap found | Main changes | Regression tests | Result |
|---|---|---|---|---|---|
| A | Safe Git baseline | No repository; 219 MB of boundary sources and a 10.7 MB .docx would have been committed | `.gitignore`, `SOURCE_ARTIFACT_MANIFEST.md`, `git init` | Staged-list and credential scan | Baseline `6800145`; no remote |
| C | Configurable retention | Raw aggregates, event UIDs and export files kept indefinitely | `domain/retention.py`, `services/purge.py`, migration 0009, `scripts/purge_expired.py`, Celery maintenance task | `test_retention_purge.py` (calendar months, dry run writes nothing, idempotency, batching, lease, stale lease, failure record, disabled) | Passed |
| D | Purge-safe provenance | Not previously proven | Denominator provenance (0010); snapshot/export reopen after purge | `test_snapshot_and_export_survive_the_deletion_of_their_raw_rows` | Passed |
| E | MPDSR minimisation | Blacklist of four names; nested and unmapped payloads could persist; flag `event_uid` never expired | `domain/mpdsr_minimisation.py` whitelist; purge clears flag UIDs | `test_mpdsr_minimisation.py` | Passed |
| F | Render-safe artifacts | Worker wrote to local disk the API could not read; expired downloads were bare 404s | `services/artifact_store.py` (`filesystem`/`database`), 410 `artifact_expired`, download re-checks permissions | `test_retention_purge.py`, `test_export_queue.py` | Passed |
| G/H | Workbook staging and reconciliation | 146 rows could not be preserved without an approved crosswalk | Migration 0011 staging, CLI `--stage`/`--write-reports` | `test_population_workbook.py`, reports in `docs/reconciliation/` | Passed (at the time reported as 143 unmatched; corrected: `reconciliation_unmatched = 143`, `non_production_candidates = 3`, `production_unresolved = 146`) |
| I/J/K | Central resolver and real populations | Sub-annual and calendar periods unresolved (now decided, D-045); resolution spread across calculation code | `resolve_target_denominator`, seeded 2024–2030 rules | `test_population_period_resolver.py` (Pader 2025 = 248,910 from fixture) | Passed |
| L | Prototype paths | None found in production code; no guard existed | Contract tests backend and frontend | `test_prototype_contract.py`, `prototype-contract.test.ts` | Passed |
| M | Boundary validation | No validation report; activation required an assumed date | `scripts/geojson_reconciliation.py`; `effective_date_verified` guard (superseded on 2026-09-15 by recorded approval references, see item 2 below) | `test_phase12_audit_fixes.py` | Passed; OBJECTID 1240 duplicate reported |
| N | Visual workspaces | All workspaces rendered one identical layout | `lib/workspaces.ts`, `QualityConsole`, `ExportHistory`, `AdministrationPanels`, `globals.css` tokens | `workspaces.test.tsx`, `e2e/workspaces.spec.ts` | Passed (Vitest 35; Playwright 12, no skips) |
| O | Development login | Username prefilled with `national.analyst`; no safe admin provisioning | Blank login; `scripts/create_initial_admin.py` | `test_admin_provisioning.py` | Passed |
| P | Neon readiness | No pool limits, TLS policy or direct migration URL | `db/session.py`, config validation, Alembic `migration_url` | `test_runtime_gates.py` | Passed |
| Q | Same-origin auth | CSRF cookie read via `document.cookie` would fail across Render hostnames | `/api` proxy (`BACKEND_INTERNAL_URL`), `render.yaml` | `test_deployment_config.py`, Playwright cookie test | Passed |
| S | DHIS2 discovery | Approximate hostname in docs; no enable switch | `DHIS2_ENABLED`/`SYNC_ENABLED`, inert discovery and refresh commands | `test_runtime_gates.py` | Passed; no network call made |
| P | Release on PostgreSQL | Revision id `0009_retention_and_artifact_storage` (35 characters) exceeded PostgreSQL `alembic_version.version_num` VARCHAR(32). SQLite does not enforce the length, so `alembic upgrade head` passed locally but would have failed at the Neon release | Revision renamed to `0009_retention_artifacts` before any deployment (0001–0008 untouched) | `test_revision_ids_fit_the_postgresql_version_column` (runs on SQLite), `test_postgres_0008_to_0009_backfills_artifact_expiry_and_downgrades` | Passed |
| C | Purge lease under contention | Lease exclusivity was only proven on SQLite | None needed | `test_postgres_purge_lease_admits_exactly_one_holder_under_contention` (6 threads, fresh and stale lease) | Passed |
| F | Stale test patch | PostgreSQL export-queue test still patched `export_jobs.os.replace`, which publication no longer uses | Patch `artifact_store.publish`; assertions unchanged | `test_postgres_export_queue_races_and_durable_failure` | Passed |
| T | Build hang | Another account (`CodexSandboxOffline`) rebuilt into `.next` during the session, re-triggering Next's infinite `EPERM` retry | Relocated output; `prebuild` fail-fast guard | Real-repository `npm run build` | exit 0 in 188 s |

## Corrective pre-UAT pass — 2026-09-15

Implemented and tested locally unless stated. "Prepared" means configured but not verified against the external service. Nothing here is owner-accepted.

| Item | Defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| A | A migrated production database had no programmes, roles or country root, so `create_initial_admin.py` could not succeed; the only seed was synthetic | `app/services/reference_bootstrap.py` + `scripts/bootstrap_reference_data.py`: approved programmes, roles/permissions, undated catalogue, quality rules, D-045 period rules, neutral `UG` root; idempotent; advisory-locked; conflicts fail closed (exit 3); refuses a non-head database (exit 2). Dev seed layers synthetic data on it. Release: migrations → bootstrap → first admin | `test_reference_bootstrap.py` (empty → head → bootstrap ×2 → admin → login; no users/geography/UIDs/raw/population/geometry; five conflict kinds; check mode), `test_postgres_reference_bootstrap.py` (same flow on PostgreSQL; five concurrent bootstraps, no NULL-programme duplicates) | Implemented, tested (SQLite + PostgreSQL) |
| B | `next.config` rewrites captured `BACKEND_INTERNAL_URL` (or the localhost fallback) at build time; Render `hostport` has no scheme; docs said the API could stay private while it was a public web service | Runtime route `/api/[...path]` with `resolveBackendTarget` (host:port → `http://`, trailing slashes stripped, non-http(s)/credentials/query rejected, production requires a value) and a streaming proxy preserving method, body, status, Cookie, X-CSRF-Token and each Set-Cookie; generic errors. Browser client fixed to `/api`. API is a Render private service | `proxy.test.ts` (31: normalisation, rejection, real HTTP backend forwarding, 3 MB streamed download, safe 502/500); `verify-proxy-build.mjs` on a real production build (0 rewrites, 0 client chunks with a backend address, hostport forwarding, `hpip-api:10000` → 502 with 0 requests to a localhost:8000 sentinel, unset → 500); Playwright `proxy-exports.spec.ts` (login, CSRF export POST, xlsx download through `/api`) | Implemented, tested locally; Render networking prepared, unverified |
| C | `render.yaml` referenced an undefined `hpip-shared` group, used `type: redis`, and ran an inert paid DHIS2 cron | Blueprint-defined non-secret group `hpip-shared-config`; secrets declared once on `hpip-api` and copied by `fromService envVarKey`; `type: keyvalue`; `pserv` API with migrations + bootstrap pre-deploy; purge cron with bounded retries; DHIS2 cron removed (documented) | `test_deployment_config.py` (references resolve, group has no secrets/sync:false, every backend process receives every required setting, identical secret sources, keys are real settings, resolved environments pass production validation) | Implemented; **not validated by Render's schema/CLI** |
| D | Alembic used `engine_from_config` without DB_SSLMODE/timeout; plain `postgresql://` needed psycopg2; migration URL TLS unchecked | `app/db/urls.py`: normalisation to `postgresql+psycopg`, driver rejection, URL-then-setting sslmode precedence with conflict detection, TLS validation for both URLs; `create_migration_engine` (NullPool, same connect args) used by `alembic/env.py`; errors never echo URLs | `test_database_urls.py` (Neon pooled/direct, special characters, psycopg2/pg8000 rejection, TLS-required/disabled/conflict for both URLs, NullPool/connect args spies; PostgreSQL: plain URL migrates, `DB_SSLMODE=require` makes Alembic fail against the non-TLS cluster, password never in the error) | Implemented, tested |
| E | Failed policies returned normally (no Celery retry); unlink `OSError` swallowed; path cleared and job deleted even when the file remained; fixed lease could expire mid-purge | File outcomes deleted/absent/failed; failed files keep path, un-deleted state and job; `PurgeFailed` raised after the safe run record; Celery retries only failed policies; CLI `--max-attempts` and non-zero exit; holder-checked lease renewal inside each batch transaction and `lease_lost` abort; operational-record retention policy | `test_purge_failures.py` (PermissionError, missing file, mixed storage, partial batches then retry, Celery retry and final failure, CLI exit/retry, lease renewal, lease loss, locked ≠ failure, operational records), `test_postgres_retention.py` (short 2 s TTL with 8 slow batches against a second session: no takeover while active; stolen lease stops deletions) | Implemented, tested |
| F | `cause_mentions` accepted short free text and analytics fell back to it | Field removed; `structured_cause_mentions` keeps only codes from an approved `CauseTaxonomy` (none configured → dropped, presentation withheld with a taxonomy reason); strict date and event-type formats; nested/numeric/oversized values dropped | `test_mpdsr_privacy_formats.py` (22 hostile values × 5 approved fields; ingestion with hostile values behind approved mappings; UID and flag reference gone after 25 h), updated `test_mpdsr_minimisation.py`, `test_amendment_mpdsr_causes.py` | Implemented, tested |
| G | Report claimed synthetic matches were not production mappings but counted 143, not 146, unresolved; staging created unlimited duplicate batches; review fields had no transition | `crosswalk_semantics`: `reconciliation_unmatched` vs `production_unresolved` (146 unless authoritative = full cohort + `POPULATION_HIERARCHY_APPROVAL_REFERENCE`); non-production candidates listed. Migration 0012 fingerprint + unique identity; staging reuses the batch (savepoint-safe under concurrency). `review_staged_batch` governed transition. Reports regenerated | `test_population_staging.py` (synthetic, unapproved and authoritative scopes; report text; reuse; new fingerprint; no populations; review rules), `test_postgres_population_staging.py` (4 concurrent stagings → 1 batch; 0011↔0012) | Implemented, tested; reports regenerated on a scratch DB |
| H | Operational but not visually accepted: 3,000+ px pages, one generic component with section flags, dozens of flags on the primary dashboard | Composition components per screen/workspace from shared panels; information-dense 16:9 layout; bounded insights with view-all; paginated flags; responsive grid; reserved crest slot | Vitest composition render tests and client-calculation contract; Playwright visual gates at 1680×945, 820×1180 and 390×844 for geography screens and all workspaces; screenshots in `docs/evidence/screenshots/` | Implemented, tested; owner visual acceptance pending |

## Corrective pre-UAT implementation — 2026-09-15 (second pass)

Implemented and tested locally. Nothing here is owner-accepted.

| Item | Defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| 1 | Bootstrap conflict detection compared only some owned fields, so drift in names, flags, permissions, version fields, rule configuration or period-rule fields was silently accepted | `reference_bootstrap._inspect` compares every bootstrap-owned field of programmes, roles (including exact permission sets), the `UG` root, indicators, current indicator versions (exactly one current version), quality rules and population-period rules before any insert. Owner-managed fields (descriptions, validity dates, ownership, facility level) and unrelated rows are ignored. Messages name fields only, never stored values. Any conflict writes nothing and exits 3 | `tests/reference_mutations.py` (29 mutations); `test_reference_bootstrap.py` parametrised per mutation on a fresh SQLite database (CLI exit 3, field named, sentinel never printed, no partial inserts, recovery after undo), owner-managed and unrelated rows accepted; `test_postgres_reference_bootstrap.py` runs every mutation on PostgreSQL | Implemented, tested (SQLite + PostgreSQL) |
| 2 | Boundary crosswalk counted synthetic matches as resolved, authority was implied by unit counts, and `--effective-date-verified` alone allowed activation | Shared `app/services/hierarchy_authority.py` (purpose × level; `authoritative_org_units` only with a full cohort **and** a configured approval reference; separate empty-by-default `BOUNDARY_*_HIERARCHY_APPROVAL_REFERENCE` settings). `boundary_crosswalk_semantics` reports `source_features`, `reconciliation_matched`, `reconciliation_unmatched`, `ambiguous`, `production_resolved`, `production_unresolved`, `non_production_candidates`, `reference_scope`, `hierarchy_approval_reference`. `apply_geometry_import` independently requires `manage_mappings`, an approved complete hierarchy at the level, complete unambiguous mappings with a mapping decision reference, a checksum, an effective date with an approval reference, same-level targets and an approval reference for any partial activation; all recorded in the audit event. Reports regenerated | `test_boundary_governance.py` (15), `test_postgres_boundary_governance.py`, updated `test_phase12_audit_fixes.py`; district/city `reconciliation_unmatched = 143`, `non_production_candidates = 3` (Kitgum, Pader, Soroti), `production_unresolved = 146`; sub-county `production_unresolved = 2,190` | Implemented, tested (SQLite + PostgreSQL); no boundary activated |
| 3 | Playwright did not return after the last test: the `next start` child inherited Playwright's stdio; ordinary runs rewrote tracked screenshots with volatile IDs | `scripts/e2e-web.mjs` pipes child output and stops only its own child (SIGTERM, bounded wait, then force). `scripts/e2e-gate.mjs` fails unless Playwright exits 0 by itself within 60 s of its summary, no test is skipped, ports 3000/8010 are closed, no observed descendant survives and `git status` is clean (superseded 2026-09-16: a pre-existing dirty tree is permitted, and status plus content fingerprint must be unchanged by the run); on a hang it terminates only the Playwright process tree it started. Screenshots go to ignored `test-results/visual-evidence/` unless `UPDATE_VISUAL_EVIDENCE=1` (`npm run e2e:evidence`); snapshot IDs and timestamps are masked | `npm run e2e:gate`: 24 passed, 0 skipped, exit 0, 0.1 s from summary to exit, ports free, no descendants, clean tree | Implemented, verified locally |
| 4 | Visual anatomy diverged from the reference (no icons, plain navigation, no alerts/profile/tagline area, no search) | Local outline icon set; grouped navigation with active pill; topbar with data-quality alert link (real open-flag count), profile, sign out and platform note; filter fields with icons; authorised search (`GET /search`, geography-path and programme scoped, literal matching, no events); panel icons, status dots, KPI direction arrows from server change values; compact downloads; pale-blue natural background; reserved crest slot kept | `test_search.py` (6); Playwright "reference layout contract at 1680x945" (DOM anatomy, positions, search journey with scope refusal); breakpoints 1680×945, 820×1180, 390×844; `docs/evidence/VISUAL_COMPARISON.md` | Implemented, tested; owner visual acceptance pending |
| 5 | Documentation equated the 143 reconciliation-unmatched units with the production-unresolved count | Exact terms: `reconciliation_unmatched = 143`, `non_production_candidates = 3`, `production_unresolved = 146` | This file, `OPEN_ITEMS.md`, `GEOJSON_BOUNDARIES.md`, `CHANGELOG_CONTEXT.md` | Corrected |

## Independent audit correction — 2026-09-15 (third pass)

This section supersedes the second-pass closure claims for bootstrap country-root handling,
authorised search and Playwright shutdown/process verification. Nothing was deployed or activated.

| Item | Reproduced defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| 1 | Search escaped the user's query but not the authorised geography path. `_` and `%` in an organisation-unit code therefore remained SQL wildcards and could return sibling-unit names | `services/search.py` uses the shared `geography.escape_like` helper and an explicit escape character for every scope-prefix predicate | `test_geography_scope_path_wildcards_are_literal` creates `/UG/A_B` and the sibling `/UG/AXB`; searching the shared term returns only the authorised subtree | Implemented; SQLite regression passed |
| 2 | Bootstrap allowed another active country root, while system-administrator landing selected an unordered country row | Bootstrap fails closed on any other active parentless country-level unit. Administrator landing independently requires the active, parentless `UG` root | SQLite country-root conflict test; deterministic administrator landing test with another country row; PostgreSQL bootstrap suite | Implemented; SQLite and PostgreSQL passed |
| 3 | All 24 Playwright cases printed `ok`, but the runner hung before its summary because restricted Windows denied Playwright's shell-tree `taskkill`. The 60-second post-summary clock never started, leaving only a 30-minute timeout | `e2e-gate.mjs` owns API, Next and Playwright as direct children; `playwright.config.ts` disables built-in web servers only for the gate. Separate 60-second post-summary, 90-second pre-summary inactivity and 600-second overall bounds cover every lifecycle stage. `npm run e2e` now uses this gate; `e2e:raw` retains the direct developer command | Full gate: 24/24, exit 0, summary-to-exit 0.1 s, services stopped, ports free. Deliberate one-second overall timeout: exit 1 with bounded cleanup | Implemented and reproduced under the previously failing account |
| 4 | `Get-CimInstance` failure was swallowed as `[]`, so the gate could claim zero descendants without enumerating them | Parent-tree enumeration remains primary. Restricted Windows falls back to a baseline/delta inventory keyed by PID plus creation time; enumeration failure is a gate failure, never a zero-process success. Git state is compared before/after rather than silently accepting new output | Full gate observed 100 created processes in fallback mode and zero survivors; forced-timeout run observed four and left zero | Implemented and verified locally |
| 5 | Review: the descendant walk had no cycle guard, so Windows PID reuse made the sampler throw `RangeError: Invalid array length` and crash the gate before cleanup (reproduced with a 30-second timeout) | Visited-set walk; a child created before its supposed parent is rejected; sampling errors become gate failures | Cyclic/PID-reuse table check; 30-second timeout rerun: exit 1, 0 survivors, ports free | Implemented, verified locally |
| 6 | Review: before/after Git comparison used status lines only, so rewriting an already-modified file passed | SHA-256 fingerprint of `git diff HEAD --binary` plus untracked file bytes, compared with the status lines; evidence refresh excludes only `docs/evidence/screenshots/` | Final `npm run e2e`: tree unchanged | Implemented, verified locally |
| 7 | Review: the production build was unbounded and unsampled; hang survivors could be stranded | Build is an owned, sampled child bounded by `E2E_BUILD_TIMEOUT_SECONDS`; verified descendants (PID + creation time) are terminated after the failure is recorded, in parent-tree mode only | Final `npm run e2e`: 114 processes observed, 0 alive | Implemented; termination branch not exercised (no survivors occurred) |

## Acceptance-gate corrections — 2026-09-16 (fourth pass)

Gate-only corrections after review rejected the third-pass gate. Logic now lives in
`frontend/scripts/e2e-gate-lib.mjs` with deterministic tests in `frontend/scripts/e2e-gate-lib.test.mjs`.

| Item | Defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| 1 | Owned roots were held by PID alone, so a reused root PID could attribute (and terminate) unrelated processes | Identity is PID plus creation time, captured immediately after spawn and revalidated in every table; exited roots discover nothing; survivors are re-matched by identity before termination | Synthetic root-PID reuse, child-PID reuse, cycles and creation ordering; termination refused after a reuse | Implemented, tested |
| 2 | A spawn failure (missing executable, access denied) was not represented in the owned outcome | `error` handler attached at spawn; the outcome carries the spawn error; the gate completes bounded cleanup, prints the summary and exits non-zero | Unit test with a nonexistent executable; full `npm run e2e` with `HPIP_PYTHON` set to a missing path | Implemented, tested |
| 3 | Process-table queries were unbounded; a hung enumerator could stall the gate | `E2E_PROCESS_QUERY_TIMEOUT_MS` (20 s) bounds every query; only the gate's own lister child is stopped; enumeration is recorded unavailable and fails closed; an in-flight sample is awaited before the summary | Hung-lister unit tests; `runGate` with a hung enumerator; full `npm run e2e` with a 50 ms bound | Implemented, tested |
| 4 | Playwright's output listener was attached after identity capture, so an early summary line could be missed | Output listeners are attached at spawn | Clean `runGate` test asserts a recorded summary time | Implemented, tested |
| 5 | Documentation claimed the gate requires an initially clean tree | Corrected: a pre-existing dirty tree is permitted; status lines and content fingerprint must be identical after the run | `LOCAL_DEVELOPMENT.md`, `CHANGELOG_CONTEXT.md`, this file | Corrected |
| 6 | Follow-up audit: initial enumeration failure recorded an error but still launched the unobservable build and services | Initial inventory is now a hard prerequisite. Failure sets `execution_started: false`, launches nothing, skips the inapplicable report-freshness check and returns a structured failed summary | Real hung-lister regression asserts zero preparation calls, zero spawns, no owned processes and no Playwright-report error; real entrypoint with a 1 ms query bound aborts before launch | Implemented, tested |
| 7 | Follow-up audit: the clean real-process test was nondeterministic under the complete Vitest suite (`102 passed, 1 failed`) because restricted-Windows baseline-delta mode saw parallel Vitest workers as new processes | Real-process tests no longer overlap other test-file workers; final survivor verification uses bounded repeated queries instead of a single timing-sensitive snapshot and reports PID, creation time, attribution and kind for persistent survivors | Gate tests **18 passed**; complete **105 passed** in three consecutive runs; full `npm run e2e` under the checkout-owning account: **24 passed, 0 skipped, 0 flaky**, natural exit 0.1 s after the summary, 102 processes observed, 0 survivors, ports free, tree unchanged | Implemented, verified locally |
| 8 | Identity capture allowed an undocumented two-second pre-spawn window on every platform | Process-table results declare their effective timestamp allowance: Windows uses 1 ms; Linux/Unix `ps lstart` needs 2,000 ms because its whole-second start time is derived from a second-resolution boot time plus jiffies | Millisecond and Linux two-second boundary tests | Implemented, tested; Linux wording corrected 2026-09-20 |

## Independent correction of demonstration and map evidence — 2026-09-20

| Item | Reproduced defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| 1 | Map layer click handlers captured the filters from the first render, so a later period/module/indicator change could drill down with stale URL state | The once-registered layer handlers read the current navigation ref at click time | `map-canvas.test.tsx` changes every filter after first paint, clicks a unit and asserts the new URL contract | Implemented, tested |
| 2 | A demo upsert matched only unit/period/key and could overwrite a current row owned by another source or programme in a non-empty development database | Demo lookup now requires `synthetic_demo_fixture`, programme, unit, period, key, null category combination and current state; duplicate demo identities fail closed | `test_demo_data.py` inserts a same-scope DHIS2 row, re-seeds, and proves its value/checksum are unchanged and no demo rows duplicate | Implemented, tested |
| 3 | The fixture omitted MR and Yellow Fever, assigned multi-programme death sources to only one programme, did not match raw mapping UIDs, and used the wall clock despite claiming deterministic output | Catalogue extraction now yields programme/source pairs; MR/YF are explicit synthetic volumes; matching raw/mapping labels are used; extraction time is the seeding moment, shared by every row and preserved on re-seed (a fixed calendar constant ages into 180 stale-reporting flags) | Catalogue-pair equality, mapping/raw identity, provenance stability across re-seeds, idempotence and production-refusal tests | Implemented, tested |
| 4 | `data-map-ready` remained true during later paints and the screenshot helper swallowed its timeout | Every update resets readiness, uses a generation-bound idle listener, and removes superseded listeners; the Playwright wait now fails on timeout | Component regression covers false → true → false → true across rerender; visual acceptance retains a strict 30 s paint bound | Implemented, tested |

## Findings exposed by the demonstration dataset — 2026-09-18

Populating the development dashboards revealed two defects that an empty dataset could not show.

| Item | Defect | Correction | Regression evidence | State |
|---|---|---|---|---|
| 1 | Calculation lineage issued one `SourceMapping` query per source row. One national immunisation dashboard ran 4,627 SQL statements (3,240 of them mapping lookups, 65% of the time) | Run-scoped memo keyed by programme, source key, mapping version, item UID, category combo and recorded mapping id; the mapping table cannot change during a run | 1,569 statements; national ANC 3.3 s → 1.4 s, immunisation 6.7 s → 2.7 s; 153 lineage/calculation tests pass unchanged | Implemented, tested |
| 2 | KPI cards could not shrink: a long status label plus the fixed 80 px sparkline widened the card's implicit grid track, so the immunisation workspace overflowed horizontally at every breakpoint | The card declares one `minmax(0, 1fr)` track; the footer wraps and its children may shrink | Playwright overflow gate at 1680×945, 820×1180 and 390×844 | Implemented, tested |
| 5 | Changing the programme module and pressing Apply submitted the previous module's indicator code. The server rejected the pair with HTTP 422 and the dashboard rendered no panels | The filter strip keeps module and indicator consistent: a module change falls back to that module's default indicator | `frontend/src/test/filter-strip.test.tsx` (drops the stale indicator on a module change, keeps it otherwise); found by browser verification of the running demonstration | Implemented, tested |
